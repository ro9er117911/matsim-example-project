================================================================================
完整 PT 映射啟動指南
================================================================================

** 重要說明 **
此映射將運行 8-10 小時，請確保：
1. 電腦不會進入睡眠模式
2. 有穩定的電源供應
3. 已執行啟動前檢查

================================================================================
步驟 1: 等待 OSM 網路重建完成
================================================================================

檢查 OSM 轉換狀態：
  tail -20 logs/osm_conversion_v2.log

確認看到類似以下訊息才能繼續：
  "INFO NetworkWriter:... done."

================================================================================
步驟 2: 執行啟動前檢查
================================================================================

cd /Users/ro9air/matsim-example-project/5000_disatar/output_full
./preflight_checks.sh

必須看到 "✅ 所有檢查通過，可以啟動映射！" 才能繼續

================================================================================
步驟 3: 在新終端啟動完整映射
================================================================================

命令（複製貼上）：

cd /Users/ro9air/matsim-example-project/5000_disatar/output_full && \
nohup java -Xmx16g \
  -cp /Users/ro9air/matsim-example-project/pt2matsim/work/pt2matsim-25.8-shaded.jar \
  org.matsim.pt2matsim.run.PublicTransitMapper \
  ptmapper-config-full-final.xml \
  > logs/ptmapper_full_final.log 2>&1 &

記錄進程 ID：
  echo $!

================================================================================
步驟 4: 啟動監控（在原終端，由 Claude 執行）
================================================================================

Claude 將自動啟動以下監控：
1. 進度監控（每 5 分鐘更新）
2. 錯誤追蹤（即時）
3. 記憶體監控（每分鐘）

================================================================================
預期時間表
================================================================================

23:50-00:00    OSM 網路重建完成
00:00-00:10    啟動前檢查 + 啟動映射
00:10-10:00    完整映射運行（9-10 小時）
10:00-10:30    驗證結果

完成時間：明早 10:00-10:30

================================================================================
成功標準
================================================================================

✅ 人工連結 < 40%（目標 < 35%）
✅ 無法到達站點 < 10
✅ 無致命錯誤
✅ 完成所有 2,362 條路線映射

================================================================================
監控檔案位置
================================================================================

主要日誌：     logs/ptmapper_full_final.log
進度記錄：     logs/progress_monitor.txt
錯誤記錄：     logs/errors_realtime.txt
記憶體記錄：   logs/memory_usage.txt

即時查看進度：
  tail -f logs/progress_monitor.txt

即時查看錯誤：
  tail -f logs/errors_realtime.txt

================================================================================
緊急停止
================================================================================

如需停止映射：
  ps aux | grep PublicTransitMapper | grep ptmapper-config-full-final
  kill <PID>

================================================================================
